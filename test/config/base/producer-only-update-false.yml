version: 1.2

input:
  masters:

  - connection:
      address: ${MYSQL_ADDR}
      port: 3306
      user: root
      password: ${MYSQL_PASS}
    onlyUpdated: false

  - connection:
      address: ${MONGO_ADDR}
      port: 27017
    type: Mongo



