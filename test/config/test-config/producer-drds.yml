version: 1.2

input:
  masters:

  - connection:
      clusterNodes: [${MYSQL_ADDR}]
      user: root
      password: ${MYSQL_OUT_PASS}


  - connection:
      address: ${MONGO_ADDR}
      port: 27017
    type: Mongo



