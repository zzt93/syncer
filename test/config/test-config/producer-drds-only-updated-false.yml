version: 1.2

input:
  masters:

    - connection:
        clusterNodes: [${MYSQL_ADDR}]
        user: root
        password: ${MYSQL_IN_PASS}
      onlyUpdated: false


    - connection:
        address: ${MONGO_IN}
        port: 27017
      type: Mongo



