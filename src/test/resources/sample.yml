syncer:

  input:
    mysql-masters:
      - connection:
          address: 192.168.1.100
          port: 1234
          user: root
          password-file: password
        schema:
          name: "*"
          tables:
            - name: affair
              row-name: [id, name]
            - name: file
              row-name: [id, name, uploader]
            - name: folder
              row-name: [id, name, uploader]

      - connection:
          address: 192.168.1.100
          port: 1235
          user: root
          password-file: password
        schema:
          name: "*"
          tables:
            - name: affair
              row-name: [id, name]
            - name: file
              row-name: [id, name, uploader]
            - name: folder
              row-name: [id, name, uploader]

#
  filter:
    if:
      condition: "%{table.name} == affair"
      actions:
        - add-field:
          - field: "time"
            value:

  output:
    elasticsearch:
      connection:
        cluster-name: elasticsearch
        cluster-nodes: ["192.168.1.100:9300"]
        user: elastic-user # optional
        password-file: es-password # optional
      input-mapping:
        index: "%{table.name}-%{time}" # default %{schema.name}
        type: "%{table.name}" # default %{table.name}
        document-id: "%{id}" # default %{id}

    http:
      connection:
        address: 192.168.1.100
        port: 9700
      output-class:

